[pytest]
testpaths = tests src/features
pythonpath = src
python_files = test_*.py test.py
norecursedirs = tests/live .git __pycache__ .venv
addopts = -p no:cacheprovider --import-mode=importlib
markers =
    xdist_group(name): keep these tests on one worker
