---
name: journal-law
description: The immutable dispatch rules the journal ships to every agent and project
---

# The law, no wigs required

The immutable dispatch rules the journal ships to every agent and project.

Always on. The laws are handed to every session, kept in AGENTS.md and CLAUDE.md, and enforced before a subagent dispatch.

It listens to: . It speaks on the events it listens to. On by default; the viewer's Settings switches it per environment, and `triggers.law` in the environment's settings tunes it.
